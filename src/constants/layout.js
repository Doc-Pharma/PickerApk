// Shared layout constants.

// Home header — top-right stack: the status badge sits at STATUS_BADGE_TOP
// and the scan button directly under it at SCAN_BUTTON_TOP (both offsets are
// added to the safe-area inset). HEADER_ACTION_INSET reserves the width the
// two occupy so the greeting text never runs beneath them.
export const HomeHeaderLayout = {
  STATUS_BADGE_TOP: 12,
  SCAN_BUTTON_TOP: 64,
  HEADER_ACTION_INSET: 90,
};
